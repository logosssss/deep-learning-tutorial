# 默认训练配置，可按教程修改
experiment_name: mnist_baseline

data:
  data_dir: data
  batch_size: 64
  num_workers: 0

model:
  name: mlp
  hidden_dim: 128
  num_classes: 10

train:
  epochs: 5
  learning_rate: 0.001
  seed: 42
  device: cuda  # 无 GPU 时改为 cpu

paths:
  checkpoint_dir: checkpoints
